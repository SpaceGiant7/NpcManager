<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.npcmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.CreateCampaignActivity"></activity>
        <activity android:name=".Activities.ViewQuestActivity" />
        <activity android:name=".Activities.ViewPersonActivity" />
        <activity android:name=".Activities.FindByNameActivity" />
        <activity android:name=".Activities.FindByRaceActivity" />
        <activity android:name=".Activities.FindByLocationActivity" />
        <activity android:name=".Activities.FindByOrganizationActivity" />
        <activity android:name=".Activities.AddOrganizationActivity" />
        <activity android:name=".Activities.AddLocationActivity" />
        <activity android:name=".Activities.FindByOccupationActivity" />
        <activity
            android:name=".Activities.AddPersonActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".Activities.AddQuestActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>